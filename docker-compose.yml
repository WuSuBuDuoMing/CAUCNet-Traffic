version: "3.8"

services:
  caucnet-traffic:
    build: .
    container_name: caucnet-traffic
    ports:
      - "3004:3004"
    environment:
      - NODE_ENV=production
      - PORT=3004
    restart: unless-stopped
